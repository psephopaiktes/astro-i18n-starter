---
layout: "@/layouts/Layout.astro"
title: {en: "Set up"}
description: {en: "This page describes the basic setup procedure for Astro i18n Starter"}
---

## {frontmatter.title.en}
{frontmatter.description.en}

```js
var elm = document.getElementById('gotop');

    window.addEventListener('scroll', function()
    if( window.pageYOffset > 200 ) elm.classList.add('show');
    else elm.classList.remove('show');

}, false);
```
